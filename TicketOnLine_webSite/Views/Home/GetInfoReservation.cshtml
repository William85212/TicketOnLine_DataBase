@model IEnumerable<TicketOnLine_webSite.Models.infoReservationWeb>

@{
    ViewData["Title"] = "GetInfoReservation";
}

<h1>GetInfoReservation</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateRepresentetion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomEvent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateRepresentetion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomEvent)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
